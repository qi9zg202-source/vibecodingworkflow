[project]
name = "vibecodingworkflow"
version = "0.1.0"
description = "VibeCoding Workflow"
requires-python = ">=3.11"
dependencies = [
    "typing-extensions>=4.8",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
